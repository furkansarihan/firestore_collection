name: firestore_collection
description: Firestore extension with cache-first, pagination and stream builder support.
version: 0.2.0
homepage: https://github.com/furkansarihan/firestore_collection

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  cloud_firestore: ^3.1.13
  rxdart: ^0.27.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: